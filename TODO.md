[{'access_token': 'self.token'}, {'Authorization': 'f'}, 2.0, 3.0, 'Acceptance', 'Status', 'SEC-3', 'File', 'src/hermesoptimizer/vault/classify.py', 13, 'Issue', 'Task', 1.0, 'tuple', '_KEY", "_TOKEN", "_SECRET', 2.0, 'Acceptance', 'Status', 'PERF-1', 'File', 'src/hermesoptimizer/sources/model_catalog.py', 322, 'Issue', 'candidates', '...', 'Task', 1.0, 'index', {'role': {'provider': 'model_name'}}, 2.0, 3.0, 'Acceptance', 'Status', 'PERF-2', 'File', 'src/hermesoptimizer/sources/model_catalog.py', 359, 'Issue', 'Task', 1.0, 'region', {'role': {'region': ['model_names]}}`\n  2. Use index in fallback path to find models available in region directly\n- **Acceptance:** Region-aware lookup becomes O(1); tests pass\n- **Status:** pending\n\n### P1 Quality (Complexity Reduction)\n\n#### QUAL-1a: Refactor `auto_update.py:run_preflight` — complexity 20 → ≤10\n- **File:** `src/hermesoptimizer/auto_update.py:103`\n- **Task:**\n  1. Extract large conditional branches into helper functions (e.g., `_check_config_matches`, `_assess_destructive_actions`)\n  2. Reduce nesting by returning early where possible\n  3. Aim for cyclomatic complexity ≤10\n- **Acceptance:** Complexity metric reduced; behavior unchanged; tests pass\n- **Status:** pending\n\n#### QUAL-1b: Refactor `auto_update.py:visit` — complexity 17 → ≤10\n- **File:** `src/hermesoptimizer/auto_update.py:115`\n- **Task:**\n  1. Extract recursive logic helpers (e.g., `_visit_dict`, `_visit_list`, `_detect_changes`)\n  2. Flatten nested if/elif chains using strategy lookup if applicable\n  3. Complexity ≤10\n- **Acceptance:** Complexity metric reduced; diff/precise behavior preserved; tests pass\n- **Status:** pending\n\n#### QUAL-1c: Refactor `loop.py:parse` — complexity 16 → ≤10\n- **File:** `src/hermesoptimizer/loop.py:157`\n- **Task:**\n  1. Extract parsing branch decisions into separate methods\n  2. Use early returns to reduce nesting\n  3. Complexity ≤10\n- **Acceptance:** Complexity metric reduced; parsing output unchanged; tests pass\n- **Status:** pending\n\n## Wave 2 — Code Health (v0.9.6 if not done here)\n\n### QUAL-2: Module docstring campaign\n- Add module-level docstring to every `src/hermesoptimizer/**/*.py` file (0% → 100% coverage)\n\n### QUAL-3: Replace print() with logging\n- Convert ~301 `print()` calls to `logging` with appropriate levels (DEBUG/INFO/WARNING/ERROR)\n\n### QUAL-4: Type hint coverage improvement\n- Increase from ~80% to ≥90% by adding missing annotations on public APIs\n\n### Additional medium-severity security items\n- CWE-377: Insecure temporary files (predictable .tmp suffixes)\n- CWE-522: Master key file permission validation\n- CWE-59: Path traversal in vault removal operations\n\n## Verification & Closeout\n\nAfter completing tasks:\n- Run `PYTHONPATH=src python -m pytest -q` — all tests must pass\n- Run `PYTHONPATH=src python -m hermesoptimizer release-readiness --dry-run` — must exit 0\n- Update `brain/active-work/current.md` with final state\n- Add final proof block to `VERSION0.9.5.md`\n- Update `CHANGELOG.md` with v0.9.5 release notes\n- Tag `v0.9.5` (if not already at commit)\n\n## Do Not Merge Yet\n\nDo not merge `dev/0.9.5` to `main` until:\n- All Wave 1 items are complete and verified\n- A full audit of the diff confirms no new critical/high issues introduced\n- Governance/docs drift checks pass']}}]