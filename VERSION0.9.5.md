{'fix': 'All critical and high-severity issues (P0)**\n- **Selected medium performance and quality issues that materially affect maintainability (P1)**\n\nWe will defer:\n- Low-severity quality items (logging', 'Security': ['SEC-1: Fix shell injection in `sources/hermes_runtime.py:51`\n- [ ] SEC-2: Move GCP OAuth2 token from query to Authorization header\n- [ ] SEC-3: Fix syntax error in `vault/classify.py:13`\n\n**P1 Performance:**\n- [ ] PERF-1: Pre-compute role→provider→model index in ModelCatalog\n- [ ] PERF-2: Build region-aware index during catalog initialization\n\n**P1 Quality (Complexity Reduction):**\n- [ ] QUAL-1a: Refactor `auto_update.py:103` — reduce `run_preflight` complexity from 20 to ≤10\n- [ ] QUAL-1b: Refactor `auto_update.py:115` — reduce `visit` complexity from 17 to ≤10\n- [ ] QUAL-1c: Refactor `loop.py:157` — reduce `parse` complexity from 16 to ≤10\n\n**Verification after each P0/P1 fix:**\n- Run targeted tests for modified module\n- Ensure no regression in adjacent functionality\n\n### Wave 2 — Medium/Low Priority (planned)\n\n- [ ] QUAL-2: Add module-level docstrings (coverage 0% → 100%)\n- [ ] QUAL-3: Replace print() with logging (301 occurrences)\n- [ ] QUAL-4: Improve type hint coverage to 90%+\n- [ ] Clean up any remaining medium security items (temporary files, path validation)\n- [ ] Address low performance items (JSON formatting, session file reads)\n\n## Exit Criteria\n\nBefore closing v0.9.5:\n- All Wave 1 items completed and tested\n- `PYTHONPATH=src python -m pytest -q` passes\n- `PYTHONPATH=src python -m hermesoptimizer release-readiness --dry-run` passes\n- `brain-doctor --dry-run` passes with current canary evidence\n- No critical or high findings remain in `brain/reports/audit-*.json`\n- `brain/active-work/current.md` updated to final v0.9.5 state\n- `VERSION0.9.5.md` final proof block added\n- `CHANGELOG.md` updated with v0.9.5 release notes\n\n## Rollback Plan\n\nIf a fix introduces instability:\n- Revert the specific change and mark the item as blocked with rationale\n- Continue with remaining items; document trade-off in closeout\n- In worst case, reset to pre-Wave 1 commit and iterate with smaller slices\n\n## Notes\n\n- Security fixes (P0) are not optional; they must land in v0.9.5\n- Performance and complexity refactors (P1) should include before/after complexity metrics\n- Documentation updates (P2) can be split to v0.9.6 if time constrained\n- Keep the test suite green throughout; add targeted tests for new code paths']}